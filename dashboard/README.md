# Rescue Command Center

This is a Next.js dashboard for visualizing rescue signals.

## Setup

1. `cd dashboard`
2. `npm install`
3. `npm run dev`

## Features

- Real-time heatmap using `react-leaflet`.
- Mock data currently hardcoded in `app/page.js`.
- Connect to Supabase in `useEffect` for live production data.
